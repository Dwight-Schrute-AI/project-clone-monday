/** @module App shell — placeholder for board selection, data loading, and layout */

function App(): React.JSX.Element {
  return (
    <div>
      <h1>Connected</h1>
    </div>
  );
}

export default App;
